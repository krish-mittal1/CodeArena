Submission ID: d26096f8-9537-47e1-8a79-a704f94a0c02
Language: java
Code:
import java.util.*;

public class Solution {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int M = sc.nextInt();
        int N = sc.nextInt();

        int[][] matrix = new int[M][N];

        for (int i = 0; i < M; i++) {
            for (int j = 0; j < N; j++) {
                matrix[i][j] = sc.nextInt();
            }
        }

        int top = 0, bottom = M - 1;
        int left = 0, right = N - 1;

        StringBuilder sb = new StringBuilder();

        while (top <= bottom && left <= right) {

            // 1️⃣ Left → Right
            for (int j = left; j <= right; j++) {
                sb.append(matrix[top][j]).append(" ");
            }
            top++;

            // 2️⃣ Top → Bottom
            for (int i = top; i <= bottom; i++) {
                sb.append(matrix[i][right]).append(" ");
            }
            right--;

            // 3️⃣ Right → Left
            if (top <= bottom) {
                for (int j = right; j >= left; j--) {
                    sb.append(matrix[bottom][j]).append(" ");
                }
                bottom--;
            }

            // 4️⃣ Bottom → Top
            if (left <= right) {
                for (int i = bottom; i >= top; i--) {
                    sb.append(matrix[i][left]).append(" ");
                }
                left++;
            }
        }

        System.out.println(sb.toString().trim());
    }
}

----------------------------------------
Error Output:
Execution engine error: NotImplementedError
